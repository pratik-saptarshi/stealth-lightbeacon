repos:
  - repo: local
    hooks:
      - id: dependency-validation
        name: Validate dependency resolution with pip-compile
        entry: python scripts/validate_dependencies.py
        language: python
        additional_dependencies:
          - pip-tools
        pass_filenames: false
        files: ^(requirements\.txt|pyproject\.toml|\.github/workflows/ci\.yml)$
